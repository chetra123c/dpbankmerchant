
import 'package:flutter/material.dart';

const colorOpacity = Color(0x00fff8f8);
const blackColor = Color(0xFF000000);
const bluepainColor = Color(0xCD8B8BF1);
const painColor = Color(0xFF01014D);
const blueColor = Color(0xFF7EA6FC);
const orangColor = Color(0xFFF8B67C);
const radColor = Color(0xFFF57A7A);
const cupleColor = Color(0xFFF87DBA);
const browColor = Color(0xFFF3C972);
const greenColor = Color(0xFF7CF67B);
const blurColor = Color(0x94E0E0E0);
const blurColor1 = Color(0x30E8E8E8);
const whiteColor = Color(0xFFFFFFFF);
const white30Color = Color(0xFFF5F1F1);
const appbarColor= Color(0xff000F44);
Decoration ? decorationStyle = BoxDecoration(
  color: whiteColor,
  borderRadius: BorderRadius.circular(5),
  boxShadow: [
    BoxShadow(
        color: Colors.black.withOpacity(0.2),
        spreadRadius: 0.5,
        blurRadius: 1,
        offset:const Offset(1,1)
    )
  ]
);
BoxShadow colorShadow =   BoxShadow(
    color: Colors.black.withOpacity(0.2),
    spreadRadius: 0.5,
    blurRadius: 1,
    offset:const Offset(1,1)
);
